name: "Check merged"
description: "Check if it has been merged."
author: aki77
inputs:
  originBranch:
    description: Origin branch
    default: staging
    required: false
  token:
    description: "Github token"
    required: true
runs:
  using: "node12"
  main: "dist/index.js"
